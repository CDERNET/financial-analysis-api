apiVersion: v1
kind: Service
metadata:
  name: financial-analysis-service
  labels:
    app: financial-analysis
spec:
  type: LoadBalancer  # External access or ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: financial-analysis